# Ensure the dynamic linker can find Homebrew-installed shared libraries
# (e.g., libxml2.so.16 for xml2, libharfbuzz for ragg).
# These paths are harmless when they do not exist (e.g., in devcontainer).
LD_LIBRARY_PATH=/home/linuxbrew/.linuxbrew/lib:${LD_LIBRARY_PATH}
